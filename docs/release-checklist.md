# Release checklist

1. Confirm `npm test` passes.
2. Confirm `npm run build` passes.
3. Update `CHANGELOG.md`.
4. Update version in `package.json`.
5. Create a git tag.
6. Publish to npm.
7. Create a GitHub release with examples.
8. Post launch note with a short demo.
